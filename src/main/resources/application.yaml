spring:
  datasource:
    url: jdbc:mysql://localhost:3306/saikiran?usessl=false
    username: root
    password: Purna@705
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

